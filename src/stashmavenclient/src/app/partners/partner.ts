export interface Partner {
    partnerId: string
    customIdentifier: string
    legalName: string
    primaryTaxIdentifier: string
    city: string
    street: string
    postalCode: string
    createdOn: string
    updatedOn: string
}